schema: https://goofy-keller-49aefb.netlify.app/graphql
generates:
  ./types/index.d.ts:
    plugins:
      - typescript
    config:
      declarationKind: 'interface'
      avoidOptionals: true
      typesSuffix: Type
  ./schema.graphql:
    plugins:
      - schema-ast
